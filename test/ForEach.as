/**
 * Example of comment.
 */
package com.finegamedesign.anagram {

    // This class is empty.
    public class EmptyClass {
        public function EmptyClass() {
            for (var i:int in new <int>[1,2,3,4]) {
                trace(i);
            }
            for each (var i:int in new <int>[1,2,3,4]) {
                trace(i);
            }
        }
    }
}
